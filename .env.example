# Browser Agent Service 环境变量模板

# [必填] API 鉴权密钥 (Bearer Token)
API_KEY=

# [可选] 最大并发会话数 (默认: 2)
MAX_SESSIONS=2

# [可选] 会话无活动自动回收时间 (秒, 默认: 900)
SESSION_TTL_SEC=900

# [可选] 快照中文本内容最大长度 (默认: 1200)
SNAPSHOT_TEXT_MAX_CHARS=1200

# [可选] 快照中最大交互元素数量 (默认: 60)
SNAPSHOT_ACTIONS_MAX=60

# [可选] 日志级别 (debug, info, warn, error, 默认: info)
LOG_LEVEL=info

# [可选] 服务监听端口 (默认: 8000)
PORT=8000

# [可选] 服务监听地址 (默认: 0.0.0.0)
HOST=0.0.0.0

# [可选] 数据存储目录 (容器内路径, 默认: /data)
DATA_DIR=/data
